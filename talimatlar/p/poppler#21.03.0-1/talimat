[paket]
tanim   = PDF oluşturma kitaplığı
paketci = Cihan_Alkan
grup    = kütüphane
url     = http://poppler.freedesktop.org

[gerek]
derleme = cmake cairo freetype tiff lcms2 curl libpng zlib openjpeg2
calisma =

[kaynak]
xz      = $url

[sha256]
#1       = 434ecbbb539c1a75955030a1c9b24c7b58200b7f68d2e4269e29acf2f8f13336

[derle]
betik   = cd $SRC
betik   = mkdir $SRC/build
betik   = cd $SRC/build
betik   = cmake ../poppler-${surum} -DCMAKE_BUILD_TYPE=Release -DCMAKE_INSTALL_PREFIX:PATH=/usr -DCMAKE_INSTALL_LIBDIR=/usr/lib -DENABLE_UNSTABLE_API_ABI_HEADERS=ON -DENABLE_GTK_DOC=OFF
betik   = make

[pakur]
betik   = cd $SRC/build
betik   = make DESTDIR="${PKG}" install

  # diğer paketlerde olanlar siliniyor
betik   = rm -vrf "${PKG}"/usr/include/poppler/{glib,qt5,qt6}
betik   = rm -vf "${PKG}"//usr/lib/libpoppler-{glib,qt5,qt6}.*
betik   = rm -vf "${PKG}"/usr/lib/pkgconfig/poppler-{glib,qt5}.pc
betik   = rm -vrf "${PKG}"/usr/{lib,share}/gir*
betik   = rm -vrf "${PKG}"/usr/share/gtk-doc

